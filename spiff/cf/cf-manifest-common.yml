---
name: CloudFoundry

director_uuid: (( merge || "BOSH_UUID" ))

releases:
- {name: cf, version: 215}

networks: (( merge ))

resource_pools: (( merge ))

jobs: (( merge ))

properties: (( merge ))
