---
name: <%= search_yaml_values("shared_properties.deployment_name") %>

director_uuid: <%= retrieve_bosh_uuid() %>

releases:
- {name: cf, version: 215}

properties:
  domain: <%= "#{search_yaml_values("terraform_outputs.env")}-cf.paas.alphagov.co.uk" %>
  system_domain: <%= "#{search_yaml_values("terraform_outputs.env")}-cf.paas.alphagov.co.uk" %>
  system_domain_organization: 'admin'
  app_domains:
    - <%= "#{search_yaml_values("terraform_outputs.env")}-cf.paas.alphagov.co.uk" %>

  haproxy: {}

  networks:
    apps: default

