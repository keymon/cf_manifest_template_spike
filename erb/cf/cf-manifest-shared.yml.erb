#
# Variables used by other stubs that are shared by all the platforms
#
---
shared_properties:
  deployment_name: <%= "#{search_yaml_values("terraform_outputs.env")}-cf" %>

job_address:
  nats:
    address: <%= "0.nats.default.#{search_yaml_values("terraform_outputs.env")}.microbosh" %>
  syslog_aggregator:
    address: <%= "0.syslog-aggregator.#{search_yaml_values("terraform_outputs.env")}.microbosh" %>
